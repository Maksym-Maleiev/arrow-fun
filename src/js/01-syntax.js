// const add = function (a, b) {
//   return a + b;
// };

/*
 * Синтаксис стрілочної функції
 * - з параметрами
 * - з одним параметром
 * - без параметрів
 */

const add = (a, b) => {
  return a + b;
};

console.log(add(2, 3));

/*
 * Явне і неявне повернення
 */

/*
 * Псевдомасив arguments
 */
